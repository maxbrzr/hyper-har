# Per-dataset runs contain checkpoints and multi-megabyte episode records.
# Keep the complete artifact tree versioned without placing its contents in Git.
artifacts/datasets/** filter=lfs diff=lfs merge=lfs -text
